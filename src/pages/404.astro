---
import StatusCode from "./status/[code].astro";
import { getStatusInfo } from "../utils/statusInfo";

const statusInfo = getStatusInfo(404);
Astro.response.status = 404;
---

<StatusCode statusInfo={statusInfo} />
